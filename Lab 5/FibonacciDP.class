import java.util.HashMap;

public class FibonacciDP {
   private static HashMap<Integer, Integer> cache = new HashMap();

   public FibonacciDP() {
   }

   public static int fibonacci(int var0) {
      if (var0 == 0) {
         return 0;
      } else if (var0 == 1) {
         return 1;
      } else if (cache.containsKey(var0)) {
         return (Integer)cache.get(var0);
      } else {
         int var1 = fibonacci(var0 - 1) + fibonacci(var0 - 2);
         cache.put(var0, var1);
         return var1;
      }
   }

   public static void main(String[] var0) {
      byte var1 = 10;
      System.out.println("Fibonacci(" + var1 + ") = " + fibonacci(var1));
   }
}
